<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.crazyit.cook2y"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".Cook2YApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
                  android:configChanges="keyboardHidden|orientation"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.VIEW"/>-->
                <!--<category android:name="android.intent.category.DEFAULT"/>-->
                <!--<category android:name="android.intent.category.BROWSABLE"/>-->
                <!--<data android:scheme="http"-->
                    <!--android:host="com.cook2y.android"/>-->
            <!--</intent-filter>-->

        </activity>
        <activity android:name=".About.AboutActivity"></activity>
        <activity android:name=".Setting.SettingActivity"></activity>
        <activity android:name=".FoodDetail.FoodDetailActivity"></activity>
        <activity android:name=".Collections.CollectionsActivity"></activity>
        <activity android:name=".Search.SearchActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permisson.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE"/>

</manifest>